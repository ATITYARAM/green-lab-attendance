# Green Lab Attendance — Astro Frontend

This directory contains the Astro frontend foundation.

## Commands

```bash
cd astro
npm install
npm run dev
npm run build
```

The existing `docs/` frontend and `worker/` deployment remain unchanged during the migration.
